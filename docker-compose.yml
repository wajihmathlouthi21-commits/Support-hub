version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/sqlite.db
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
